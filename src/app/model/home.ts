export declare interface Tabs{
    title: string,
    link: string
}

export declare interface Slider{
    imgUrl: string,
    caption: string
}

export declare interface Channel{
    id:number,
    url?: string,
    title: string
}